class CUSTOMER {
	private root = '/customer'

	HOME = this.root

	PROFILE = `${this.root}/profile`
	CREATE = `${this.root}/create`
	HISTORY = `${this.root}/history`
}
export const CUSTOMER_PAGES = new CUSTOMER()
