class EXECUTOR {
	private root = '/executor'

	HOME = this.root

	PROFILE = `${this.root}/profile`
	CREATE = `${this.root}/create`
	HISTORY = `${this.root}/history`
}
export const EXECUTOR_PAGES = new EXECUTOR()
